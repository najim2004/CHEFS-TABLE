
import PropTypes from 'prop-types';
import Navbar from '../Navbar/Navbar';

const Header = props => {
    return (
        <div>
            <Navbar></Navbar>
        </div>
    );
};

Header.propTypes = {
    
};

export default Header;